#############################################
##### Testing
#############################################

set(prefix "${MODULE}")

add_test(
    NAME ${prefix}_can_Run
    COMMAND ${APPLICATION}
)
set_tests_properties (${prefix}_can_Run PROPERTIES
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_incorrect_input
    COMMAND ${APPLICATION} sdsfp 02 asd5
)
set_tests_properties (${prefix}_Validation_incorrect_input PROPERTIES
    PASS_REGULAR_EXPRESSION "Incorrect input."
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validate_less_arguments
    COMMAND ${APPLICATION} sp
)
set_tests_properties (${prefix}_Validate_less_arguments PROPERTIES
    PASS_REGULAR_EXPRESSION "Incorrect input."
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_many_arguments
    COMMAND ${APPLICATION} sp 02 asd5 2 32 13
)
set_tests_properties (${prefix}_Validation_many_arguments PROPERTIES
    PASS_REGULAR_EXPRESSION "Incorrect input."
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_when_first_vertex_is_incorrect
    COMMAND ${APPLICATION} sp -9 3
)
set_tests_properties (${prefix}_Validation_when_first_vertex_is_incorrect PROPERTIES
    PASS_REGULAR_EXPRESSION "Error with argument 1: not a number"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_when_second_vertex_is_incorrect
    COMMAND ${APPLICATION} sp 1 -7
)
set_tests_properties (${prefix}_Validation_when_second_vertex_is_incorrect PROPERTIES
    PASS_REGULAR_EXPRESSION "Error with argument 2: not a number"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_when_both_vertex_is_incorrect
    COMMAND ${APPLICATION} sp -99 -7
)
set_tests_properties (${prefix}_Validation_when_both_vertex_is_incorrect PROPERTIES
    PASS_REGULAR_EXPRESSION "Error with argument 1: not a number"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_help_information_after_main_command_without_arguments
    COMMAND ${APPLICATION}
)
set_tests_properties (${prefix}_Validation_help_information_after_main_command_without_arguments PROPERTIES
    PASS_REGULAR_EXPRESSION "Dijckstra's Algorithm.\n"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Validation_help_information
    COMMAND ${APPLICATION} help
)
set_tests_properties (${prefix}_Validation_help_information PROPERTIES
    PASS_REGULAR_EXPRESSION "Dijckstra's Algorithm.\n"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Shortest_path_between_two_vertex_which_equals
    COMMAND ${APPLICATION} sp 2 2
)
set_tests_properties (${prefix}_Shortest_path_between_two_vertex_which_equals PROPERTIES
    PASS_REGULAR_EXPRESSION "0"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Cant_do_work_without_vertexes
    COMMAND ${APPLICATION} init graph 0
)
set_tests_properties (${prefix}_Cant_do_work_without_vertexes PROPERTIES
    PASS_REGULAR_EXPRESSION "Error: graph has no vertexes"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Can_init_very_big_graph
    COMMAND ${APPLICATION} init graph 100
)
set_tests_properties (${prefix}_Can_init_very_big_graph PROPERTIES
    PASS_REGULAR_EXPRESSION "Graph successfully initialized"
    LABELS "${MODULE}")

add_test(
    NAME ${prefix}_Cant_init_very_big_graph_threshold
    COMMAND ${APPLICATION} init graph 101
)
set_tests_properties (${prefix}_Cant_init_very_big_graph_threshold PROPERTIES
    PASS_REGULAR_EXPRESSION "Error with argument 2: too big number"
    LABELS "${MODULE}")
